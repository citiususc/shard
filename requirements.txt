# br2shacl-ui — self-contained demo dependencies
#
# Core (always required)
rdflib>=7.0
python-dotenv>=1.0

# Ontology parsing for RDF/XML (.owl/.rdf) support
rdflib-jsonld; python_version < "3.0"   # no-op placeholder on py3; rdflib handles xml natively

# Mode A + Mode B inference (Databricks via the vendored model_loader)
openai>=1.30
httpx>=0.27
langchain>=0.2
langchain-core>=0.2
langchain-community>=0.2
langchain-chroma>=0.1
chromadb>=0.5

# Mode B guide preprocessing
beautifulsoup4>=4.12
Pillow>=10.0
unstructured>=0.14
pdfminer.six>=20221105     # best-effort PDF → text for uploaded PDF guides

# Optional — only needed to run inference LOCALLY on a GPU via HuggingFace
# (the demo defaults to Databricks and does NOT require these):
# torch
# transformers
# accelerate
